build:
  docker:
    web: Dockerfile
  config:
    APP_ENV: production
    MONGO_DATABASE: vulcano
    MONGO_USER: "< %= ENV['MONGO_USER'] %>"
    MONGO_PASSWORD: $MONGO_PASSWORD
    MONGO_SRV: "< %= ENV['MONGO_SRV'] %>"